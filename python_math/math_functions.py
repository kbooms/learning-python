
x = 3.14
y = 4
z = 5

# round() rounds the contained number to the nearest whole number
# result = round(x)

# abs() returns the distance from 0, the absolute value, as a whole number
# result = abs(y)

# pow() uses two parameters, first value is a number, second value is an exponential power
# result = pow(y, 3)

# max() returns the highest value contained within a set of numbers
# result = max(x, y, z)

# min() returns the lowest value contained within a set of numbers
result = min(x, y, z)

print(result)